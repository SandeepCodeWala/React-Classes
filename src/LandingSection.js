import React from "react";
import "./LandingSection.css";
import bgImage from "./xx.png"; // Import the image

function LandingSection() {
  return (
    <div className="landing-section" style={{ backgroundImage: `url(${bgImage})` }}>
    
    </div>
  );
}

export default LandingSection;
